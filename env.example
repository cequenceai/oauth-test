# OAuth 2.0 Flow Tester Configuration
# Copy this file to .env and update with your values

# ========================================
# Authorization Code Flow Configuration
# ========================================

# OAuth Provider Endpoints
AUTHORIZATION_URL=https://your-auth-provider.com/oauth/authorize
TOKEN_URL=https://your-auth-provider.com/oauth/token

# Client Credentials
CLIENT_ID=your-client-id
# Optional - leave empty for public clients (e.g. PKCE-only flows)
CLIENT_SECRET=

# OAuth Settings
SCOPES=openid profile email
REDIRECT_URI=http://localhost:5002/callback

# Security Settings
USE_PKCE=true
CLIENT_AUTH_METHOD=client_secret_post
VERIFY_SSL=true

# ========================================
# Advanced OAuth Parameters (Optional)
# ========================================

# Authentication Context Class Reference
ACR_VALUES=

# Prompt parameter (login, consent, select_account, none)
PROMPT=

# Login hint for pre-filling username
LOGIN_HINT=

# Nonce value for additional security
NONCE=

# Custom request parameters, one key=value per line (also editable in the UI).
# Use a double-quoted multi-line value for more than one param, e.g.:
# AUTH_EXTRA_PARAMS="resource=https://aws-mcp.us-east-1.api.aws/mcp
# audience=my-api"
AUTH_EXTRA_PARAMS=
TOKEN_EXTRA_PARAMS=

# ========================================
# Dynamic Client Registration (DCR)
# ========================================

# DCR Endpoint
REGISTRATION_ENDPOINT=https://your-auth-provider.com/oauth/register

# Initial Access Token (if required by provider)
INITIAL_ACCESS_TOKEN=

# ========================================
# Provider-Specific Examples
# ========================================

# Example: Auth0
# AUTHORIZATION_URL=https://YOUR_DOMAIN.auth0.com/authorize
# TOKEN_URL=https://YOUR_DOMAIN.auth0.com/oauth/token
# REGISTRATION_ENDPOINT=https://YOUR_DOMAIN.auth0.com/oidc/register

# Example: Okta
# AUTHORIZATION_URL=https://YOUR_DOMAIN.okta.com/oauth2/v1/authorize
# TOKEN_URL=https://YOUR_DOMAIN.okta.com/oauth2/v1/token
# REGISTRATION_ENDPOINT=https://YOUR_DOMAIN.okta.com/oauth2/v1/clients

# Example: Keycloak
# AUTHORIZATION_URL=https://YOUR_KEYCLOAK/auth/realms/YOUR_REALM/protocol/openid-connect/auth
# TOKEN_URL=https://YOUR_KEYCLOAK/auth/realms/YOUR_REALM/protocol/openid-connect/token
# REGISTRATION_ENDPOINT=https://YOUR_KEYCLOAK/auth/realms/YOUR_REALM/clients-registrations/openid-connect

# Example: Google (OAuth 2.0)
# AUTHORIZATION_URL=https://accounts.google.com/o/oauth2/v2/auth
# TOKEN_URL=https://oauth2.googleapis.com/token
# Note: Google does not support standard DCR

# Example: Microsoft Azure AD
# AUTHORIZATION_URL=https://login.microsoftonline.com/YOUR_TENANT_ID/oauth2/v2.0/authorize
# TOKEN_URL=https://login.microsoftonline.com/YOUR_TENANT_ID/oauth2/v2.0/token
# Note: Azure AD uses app registration portal instead of DCR
